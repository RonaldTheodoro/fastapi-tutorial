[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = {extras = ["all"], version = "*"}

[dev-packages]
pytest = "*"

[requires]
python_version = "3.8"

[scripts]
server = "uvicorn app:app --reload"
